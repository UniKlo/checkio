def checkio(data):
  import re
  for i in data:
     # if len(data)> 9 and
  if any(i.isalpha()) is True:
    return True
           # if True:
            #    re.search('[A-Z]', data)
            #    if True:
             #       re.search('[0-9]', data)
                    
      else:
        return False

#Some hints
#Just check all conditions
