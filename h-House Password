def checkio(data):
    import re
    if len(data)> 9 and bool(re.search(r'\d', data)) is True and bool(re.search('[A-Z]', data)) is True and not data.islower() and not data.isupper():
        return True
    
    else:
        return False

